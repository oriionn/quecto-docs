---
title: "Standalone"
description: "Host Quecto with bun runtime"
---
import { SiBun } from "@icons-pack/react-simple-icons";
import { Step, Steps } from 'fumadocs-ui/components/steps';

## Requirements [#requirements]
<Cards>
    <Card title="Bun" icon={<SiBun color="#FEF0DF" />} className="card-vertical" />
</Cards>

## Installation [#installation]

<Steps>
    <Step>
        ### Clone the repository [#clone]
        Execute the following command to clone the repository.
        ```bash
        git clone https://github.com/oriionn/quecto.git
        ```
    </Step>

    <Step>
        ### Install dependencies [#dependencies]
        Execute following commands to install the dependencies.
        ```bash
        cd quecto
        bun install
        ```
    </Step>

    <Step>
        ### Build the project [#build]
        Execute the following command to build the project.
        ```bash
        bun run build
        ```
    </Step>

    <Step>
        ### Generate the database and configuration files [#generate]
        Execute the following command to generate the database and configuration files.
        ```bash
        bun generate:config && bun generate:db
        ```
    </Step>

    <Step>
        ### Configuration [#config]
        Edit the `config.yml` file in the `data` directory.

        For more information, see the [Configuration](docs/config) page.
    </Step>

    <Step>
        ### Run the project [#run]
        Execute the following command to run the project.
        ```bash
        bun start
        ```
    </Step>
</Steps>

Once you have completed these steps, you can access your Quecto instance at `http://localhost:3000`.